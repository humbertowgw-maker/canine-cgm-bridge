# configuration

Shared veterinary preset parameters (target BG ranges, calibration bootstrap defaults,
analytics thresholds) loaded by both `mobile-bridge` and `cloud-backend` via
`configuration.loader.load_presets()`.
