.venv/
__pycache__/
*.pyc
*.db
*.db-journal
*.db-wal
*.db-shm
.pytest_cache/
*.egg-info/
.DS_Store

# ESP-IDF (hardware-bridge)
build/
managed_components/
dependencies.lock
# sdkconfig holds the plaintext WiFi password (CGM_WIFI_PASSWORD) once configured
# via `idf.py menuconfig` -- never commit it. Each developer runs menuconfig locally.
sdkconfig
sdkconfig.old
